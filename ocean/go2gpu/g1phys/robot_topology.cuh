// Go2-specific robot selector (this copy lives under ocean/go2gpu/, so it
// defaults to the Go2 topology — no -DROBOT_TOPO_H needed at build time).
#pragma once
#ifndef ROBOT_TOPO_H
#define ROBOT_TOPO_H "go2_topology.cuh"
#endif
#include ROBOT_TOPO_H
